r=float(input("Enter the radius"))
pi=22/7
area=pi*r**2#we can also use area=math.pi*r**2 to get the value of pi from the inbuild python library(pi is an inbuilt module)
print(area) #to use such libraries we must first have it imported in our program by using <import math> syntax
