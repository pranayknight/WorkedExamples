import logging

logging.error("Error")
logging.critical("Cricitical")   #since these two are above warning it will be shown in consoe while the ones after warning will not be shown
logging.warning("Warning")
logging.debug("Debug")
logging.info("Info")
